<h2 class="page-header">Edit Account</h2>
<%= form_tag admin_account_path, method: :patch do %>
  <%= error_messages_for(@current_admin) %>
  <div class="form-group">
    <%= label_tag :username %>
    <%= text_field_tag :username, params[:username], class: 'form-control', autofocus: true %>  
  </div>
  <div class="form-group">
    <%= label_tag :current_password %>
    <%= password_field_tag :current_password, params[:current_password], class: 'form-control', autocomplete: 'off' %>
  </div>
  <div class="form-group">
    <%= label_tag :new_password %>
    <%= password_field_tag :new_password, params[:new_password], class: 'form-control', autocomplete: 'off' %>
  </div>
  <div class="form-group">
    <%= submit_tag 'Update', class: 'btn btn-primary' %>
  </div>
<% end %>
<div class="row">
  <div class="col-md-12">
    <h2 class="page-header">
      <span>Clients</span>
      <span class="pull-right">
        <%= link_to new_admin_client_path, class: 'btn btn-primary' do %>
          <span class="glyphicon glyphicon-plus"></span> New
        <% end %>
      </span>
    </h2>
  </div>  <!-- .col-md-12 -->
</div> <!-- .row -->
<table class="table table-bordered table-striped table-master content">
  <thead>
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Description</th>
      <th colspan="2">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @clients.each_with_index do |client, index| %>
      <tr>
        <td><%= index += 1 %></td>
        <td><%= link_to client.full_name, admin_client_path(client), title: 'View', data: { toggle: 'tooltip' } %></td>
        <td><%= client.description %></td>
        <td>
          <%= link_to edit_admin_client_path(client), title: 'Edit', data: { toggle: 'tooltip'} do %>
            <span class="glyphicon glyphicon-pencil"></span>
          <% end %>
          <%= link_to admin_client_path(client), method: :delete,
                title: 'Delete',
                data: { toggle: 'tooltip', confirm: 'Do you really want to delete this record?' } do %>
            <span class="glyphicon glyphicon-remove"></span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
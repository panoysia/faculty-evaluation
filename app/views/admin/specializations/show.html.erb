<h2 class="page-header"><%= @specialization.name %></h2>
<h4 class="page-header career-paths-subheader">Faculty members who specialize under this:</h4>
<table class="table table-bordered table-striped table-master content">
  <thead>
    <tr>
      <th>#</th>
      <th>Faculty</th>
    </tr>
  </thead>
  <tbody>
    <% @employees.each_with_index do |employee, index| %>
      <tr>
        <td><%= index += 1 %></td>
        <td><%= link_to employee.full_name, admin_employee_path(employee) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="links">
  <%= link_to 'Edit', [:edit, :admin, @specialization], class: 'btn btn-link' %> |
  <%= link_to 'Back to List', admin_specializations_path, class: 'btn btn-link' %>
</div>
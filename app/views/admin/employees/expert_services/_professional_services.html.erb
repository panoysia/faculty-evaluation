<table class="table table-striped table-bordered table-master">
  <thead>
    <tr>
      <th>#</th>
      <th>Title</th>
      <th>Service Type</th>
      <th>Level</th>
      <th>Participation</th>
      <th>Sponsoring Agency</th>
      <th>Start</th>
      <th>End</th>
      <th colspan="2">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @employee.professional_services.each_with_index do |service, index| %>
      <tr>
        <td><%= index += 1 %></td>
        <td><%= service.title %></td>
        <td><%= service.service_type_to_string %></td>
        <td><%= service.level_to_string %></td>
        <td><%= service.nature_of_participation %></td>
        <td><%= service.sponsoring_agency %></td>
        <td><%= date_format_readable(service.start_at) %></td>
        <td><%= date_format_readable(service.end_at) %></td>
        <td>
          <%= link_to edit_admin_employee_professional_service_path(
                        @employee, service
                        ), 
                title: 'Edit', data: { toggle: 'tooltip' } do %>
            <span class="glyphicon glyphicon-pencil"></span>
          <% end %>
          <%= link_to admin_employee_professional_service_path(
                        @employee, service
                        ),
                method: :delete, title: 'Delete',
                data: { toggle: 'tooltip', 
                        confirm: 'Do you really want to delete this record?' } do %>
            <span class="glyphicon glyphicon-remove"></span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>